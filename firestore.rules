rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write on the users collection
    match /users/{userId} {
      allow read, write: if true;
    }
    
    // Catch-all rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
